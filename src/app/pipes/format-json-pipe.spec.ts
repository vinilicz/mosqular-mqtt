import { FormatJsonPipe } from './format-json-pipe';

describe('FormatJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
