import { MessageDiffPipe } from './message-diff-pipe';

describe('MessageDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
